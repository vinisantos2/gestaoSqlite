export class Financa {
    id: number;
    valor: number;
    data: string;
    nome: string;
    constructor(id: number, valor: number, data: string, nome: string) {
        this.id = id;
        this.valor = valor;
        this.data = data;
        this.nome = nome
    }
}